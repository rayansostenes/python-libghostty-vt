"""Type stub for the generated cffi raw layer (``ghostty_vt._raw``).

The extension is produced at build time by ``_cffi_build.py`` and has no static
types of its own. cffi's ``ffi``/``lib`` surfaces are inherently dynamic, so this
stub keeps the idiomatic layer importable and type-checkable without asserting a
false structure over the C boundary.
"""

from typing import Any

ffi: Any
lib: Any
